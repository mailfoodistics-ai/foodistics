-- Admin User Setup - SQL Only

To create admin user, simply run the SQL from:
`sql/create-admin-user.sql`

This is now a SQL-only setup with automatic UUID generation.
No TypeScript script needed!

Admin Login URL: /auth/admin-signin
Default Email: admin@foodistics.com
